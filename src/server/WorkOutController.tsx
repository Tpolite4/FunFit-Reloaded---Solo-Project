import { Request, Response, NextFunction, RequestHandler } from 'express';
import fs from 'fs';
import path from 'path';

import { workOut, workOutSchema } from './WorkOutModel';

export const workOutController = {
  getWorkOuts: () => {},

  updateWorkOuts: () => {},

  addWorkOuts: () => {},

  deleteWorkOuts: () => {},
};
